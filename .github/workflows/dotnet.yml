# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    environment: Dev

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal
      env:
        OpenAI_Endpoint: ${{vars.OPENAI_ENDPOINT}}
        OpenAI_Key: ${{secrets.OPENAI_KEY}}
        OpenAI_AudioService_Endpoint: ${{vars.OPENAI_AUDIOSERVICE_ENDPOINT}}
        OpenAI_AudioService_Key: ${{secrets.OPENAI_AUDIOSERVICE_KEY}}
        OpenAI_ImageService_Endpoint: ${{vars.OPENAI_IMAGESERVICE_ENDPOINT}}
        OpenAI_ImageService_Key: ${{secrets.OPENAI_IMAGESERVICE_KEY}}
        OpenAI_TranslationService_Endpoint: ${{vars.OPENAI_TRANSLATIONSERVICE_ENDPOINT}}
        OpenAI_TranslationService_Key: ${{secrets.OPENAI_TRANSLATIONSERVICE_KEY}}
    