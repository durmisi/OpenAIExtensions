# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    environment: Dev

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal
      env:
        ASPNETCORE_OpenAI_Endpoint: ${{vars.OPENAI_ENDPOINT}}
        ASPNETCORE_OpenAI_Key: ${{secrets.OPENAI_KEY}}
        ASPNETCORE_OpenAI_AudioService_Endpoint: ${{vars.OPENAI_AUDIOSERVICE_ENDPOINT}}
        ASPNETCORE_OpenAI_AudioService_Key: ${{secrets.OPENAI_AUDIOSERVICE_KEY}}
        ASPNETCORE_OpenAI_ImageService_Endpoint: ${{vars.OPENAI_IMAGESERVICE_ENDPOINT}}
        ASPNETCORE_OpenAI_ImageService_Key: ${{secrets.OPENAI_IMAGESERVICE_KEY}}
        ASPNETCORE_OpenAI_TranslationService_Endpoint: ${{vars.OPENAI_TRANSLATIONSERVICE_ENDPOINT}}
        ASPNETCORE_OpenAI_TranslationService_Key: ${{secrets.OPENAI_TRANSLATIONSERVICE_KEY}}
    